# =============================================
# 환경변수 예시
# 실제 파일은 .env, .env.local, .env.development, .env.production 구조를 사용합니다.
# =============================================

# ============================================= 
# .env.example
# ============================================= 
# ── 앱 설정 ──────────────────────────────────
NEXT_PUBLIC_APP_NAME="My App"

# ── API 기본값 ───────────────────────────────
# 서버 전용 API URL입니다. 브라우저에 노출되지 않습니다.
API_BASE_URL=http://localhost:8080/api

# 브라우저에서 접근 가능한 API URL입니다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# ── Mock API ────────────────────────────────
# true: src/mocks 데이터 사용, false: 실제 API 호출
NEXT_PUBLIC_USE_MOCK_API=true


# ============================================= 
# .env.local|development|production.example
# ============================================= 
# ── 앱 환경 ──────────────────────────────────
NEXT_PUBLIC_APP_ENV=local

# ── API ─────────────────────────────────────
# 서버 전용 API URL입니다. 브라우저에 노출되지 않습니다.
API_BASE_URL=http://localhost:8080/api

# 브라우저에서 접근 가능한 API URL입니다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# ── Mock API ────────────────────────────────
# 로컬에서는 mock API 사용을 기본으로 합니다.
NEXT_PUBLIC_USE_MOCK_API=true

# ── 외부 서비스 ───────────────────────────────
# NEXT_PUBLIC_ prefix가 붙은 값은 브라우저 번들에 포함됩니다.
NEXT_PUBLIC_KAKAO_MAP_KEY=your_local_kakao_map_key
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=

# ── 서버 비밀값 ──────────────────────────────
# 브라우저에 노출되면 안 되는 값은 NEXT_PUBLIC_ prefix를 붙이지 않습니다.
SOME_SECRET_API_KEY=secret_key_not_exposed_to_browser

# ── 다국어 저장소 ────────────────────────────
# file: 서버의 특정 폴더에서 JSON 파일을 읽고 씁니다.
# s3: AWS S3에서 JSON 파일을 읽고 씁니다.
I18N_STORAGE=file
I18N_LOCALES_DIR=public/locales

# I18N_STORAGE=s3 일 때 필요합니다.
AWS_REGION=ap-northeast-2
I18N_S3_BUCKET=
I18N_S3_PREFIX=locales
